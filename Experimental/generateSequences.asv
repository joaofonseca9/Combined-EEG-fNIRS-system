numbers = [1, 2, 3, 4];
prevElement = 0;
for i=1:12
    while true
        newElement = randsample(numbers, 1);
        if newElement != prevElement
            break
    end

end

newseq1 = numbers(randperm(length(numbers)));